import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.preventivpro.app',
  appName: 'PreventivPRO',
  server: {
    url: 'https://preventivpro-app.vercel.app',
  },
};

export default config;
